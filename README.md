# The Z — بوت ديسكورد إداري متكامل + لوحة تحكم ويب

بوت ديسكورد شامل باللغة العربية (Discord.js v14 + TypeScript) مع لوحة تحكم ويب تفاعلية
(Next.js 14 App Router) تمنح مدير السيرفر تحكماً كاملاً بكل ميزة — بدون الحاجة لتعديل الكود.
كل الأوامر تدعم **Slash Commands** و**البادئة النصية** معاً، وكل الرسائل والقوائم قابلة للتخصيص من اللوحة.

## 📁 هيكل المشروع (Monorepo)

```
the_z/
├── shared/     # حزمة مشتركة: نماذج قاعدة البيانات (Mongoose) + أدوات مشتركة بين البوت واللوحة
├── bot/        # بوت الديسكورد (discord.js v14)
└── dashboard/  # لوحة التحكم (Next.js 14 App Router)
```

## ✨ الميزات المُنفَّذة

| النظام | الوصف |
|---|---|
| 🎫 التذاكر | فتح عبر أزرار/قائمة، تصنيفات متعددة، أسئلة مسبقة، استلام/إغلاق، Transcript بصيغة HTML |
| 🎙️ الرومات الصوتية المؤقتة | Join-to-Create، لوحة تحكم كاملة (قفل/إخفاء/طرد/نقل الملكية/تسمية...) |
| 🎨 رولات الألوان | قائمة ألوان تفاعلية مع تقييد حسب الرتب |
| 🎖️ الرولات الذاتية | لوحات Self-Roles (أزرار/قائمة) |
| 😊 رولات الرياكشن | منح/سحب الرتب عبر الرياكشن على رسائل محددة |
| 👋 الترحيب والمغادرة | نص/Embed/صورة تلقائية (Canvas)، إرسال بالخاص أو بروم محدد |
| 🤖 الردود التلقائية | كلمة مفتاحية/تطابق كامل/Regex، رد نصي أو Embed أو أزرار |
| 🆙 المستويات والخبرة | XP نصي وصوتي (بما فيها الرومات الدائمة)، بطاقة رتبة مُصمَّمة (Canvas)، جوائز رتب، Leaderboard |
| 📋 اللوق الشامل | رومات لوق مستقلة لكل نوع حدث (رسائل، أعضاء، رتب، رومات، إشراف، ملفات/وسائط) |
| 🛡️ الرقابة التلقائية | حظر روابط/سبام/كابس لوك/تكرار حروف/سب/منشن مكثف |
| 🚨 مكافحة الغزو (Anti-Nuke) | حدود لحظر/طرد/حذف-إنشاء رومات ورتب خلال فترة زمنية + عقوبة فورية |
| 🔐 نظام التحقق (Captcha) | زر تحقق + رتبة مؤقتة + طرد تلقائي عند عدم التحقق |
| ⛓️ نظام السجن | سجن مؤقت (إزالة الرتب واستعادتها تلقائياً عند انتهاء المدة)، أوامر `,jail`/`,unjail` |
| 🔢 عداد الأعضاء | رومات/قنوات عداد تتحدث تلقائياً بعدد الأعضاء/البوتات |
| ⏰ الرسائل المجدولة | رسائل متكررة (كل N دقيقة/يومياً) أو لمرة واحدة |
| 📊 سجل الإشراف | تدقيق كامل لأوامر الإشراف (بان/كيرك/ميوث/تحذير/قفل...) في اللوحة |
| 💬 الأوامر العامة | Ping، معلومات السيرفر/العضو، صورة وبانر المستخدم، ترجمة، تصويت، اقتراحات، AFK |
| 🛠️ أوامر الإشراف | Ban، Kick، Mute، Unmute، Warn، Warnings، Clear، Lock/Unlock، Slowmode |
| 🌐 لوحة تحكم كاملة | OAuth2، اختيار سيرفر، تعديل كل الرسائل والمتغيرات، إدارة الأوامر والرتب، Dark Mode |

## ⚙️ المتطلبات

- Node.js 20 أو أحدث
- قاعدة بيانات MongoDB (محلية أو Atlas أو Render MongoDB)
- تطبيق Discord (Bot + OAuth2) من [Discord Developer Portal](https://discord.com/developers/applications)
- (اختياري) Redis — عند غيابه يعمل البوت على ذاكرة العملية للبيانات المؤقتة فقط

## 🔑 إعداد تطبيق Discord

1. أنشئ تطبيقاً جديداً في [Discord Developer Portal](https://discord.com/developers/applications).
2. من تبويب **Bot**: أنشئ البوت، وفعّل الـ Privileged Gateway Intents الثلاثة:
   - `PRESENCE INTENT`
   - `SERVER MEMBERS INTENT`
   - `MESSAGE CONTENT INTENT`
   - انسخ الـ **Token**.
3. من تبويب **OAuth2 → General**: انسخ **Client ID** و**Client Secret**، وأضف Redirect URL التالي:
   `http://localhost:3000/api/auth/callback/discord`
4. لدعوة البوت لسيرفرك، استخدم OAuth2 URL Generator بصلاحيات `bot` + `applications.commands`
   مع صلاحية Administrator (أو الصلاحيات المطلوبة يدوياً حسب الميزات التي ستُفعّلها).

## 🧩 التثبيت

```bash
# من جذر المشروع (يثبت الحزم الثلاث لأنها Workspaces)
npm install

# ملفات البيئة
cp bot/.env.example bot/.env
cp dashboard/.env.local.example dashboard/.env.local
```

عبّئ القيم التالية:

**`bot/.env`**
```
DISCORD_TOKEN=توكن البوت
DISCORD_CLIENT_ID=آيدي التطبيق
DISCORD_CLIENT_SECRET=سر التطبيق
DATABASE_URL=mongodb://127.0.0.1:27017/thez_bot
DEV_GUILD_ID=آيدي سيرفر التجربة (اختياري، لتسجيل فوري للأوامر أثناء التطوير)
```

**`dashboard/.env.local`**
```
DISCORD_CLIENT_ID=نفس آيدي التطبيق
DISCORD_CLIENT_SECRET=نفس سر التطبيق
DISCORD_BOT_TOKEN=نفس توكن البوت
NEXTAUTH_SECRET=قيمة عشوائية طويلة (يمكن توليدها بـ: openssl rand -base64 32)
NEXTAUTH_URL=http://localhost:3000
DATABASE_URL=mongodb://127.0.0.1:27017/thez_bot
```

## 🏗️ البناء والتشغيل (تطوير)

```bash
# 1) ابنِ الحزمة المشتركة أولاً (وكل مرة تعدّل فيها)
npm run build:shared

# 2) سجّل أوامر Slash على ديسكورد
npm run deploy:commands

# 3) شغّل البوت (وضع تطوير مع إعادة تشغيل تلقائية)
npm run dev:bot

# 4) شغّل لوحة التحكم (في نافذة طرفية أخرى)
npm run dev:dashboard
```

افتح لوحة التحكم على `http://localhost:3000`.

### أوامر إضافية
```bash
npm run build:bot        # بناء البوت للإنتاج (dist/)
npm run build:dashboard  # بناء لوحة التحكم للإنتاج
npm run clear:commands   # حذف كل أوامر Slash العالمية من ديسكورد
npm run start            # تشغيل البيئة الكاملة (health → deploy → bot → dashboard)
```

## 🚀 النشر (Render)

المشروع جاهز للنشر على [Render](https://render.com) عبر Dockerfile و`render.yaml`:

1. أنشئ Web Service من المستودع (Environment: **Docker**).
2. ضبط المتغيرات في Render:
   - `DISCORD_TOKEN`، `DISCORD_CLIENT_ID`، `DISCORD_CLIENT_SECRET`، `DISCORD_BOT_TOKEN`
   - `DATABASE_URL` (يفضَّل Render MongoDB مع شهادة SSL)
   - `NEXTAUTH_URL=https://<اسم-الخدمة>.onrender.com`
   - `NEXTAUTH_SECRET` (يُولَّد تلقائياً)
   - `REDIS_URL` (اختياري)
3. أضف قرصاً مضمّناً (Disk) بمقاس 1GB مثبّتاً على `/app/certs` لشهادة قاعدة البيانات.
4. يشغّل `start.js` التسلسل تلقائياً: فحص اتصال Discord ← نشر الأوامر ← تشغيل البوت ← تشغيل لوحة التحكم،
   مع خادم صحة على المنفذ المحدد حتى لا يقطع Render الخدمة.

> **ملاحظة:** عند إضافة/إزالة أوامر من مجلد `bot/src/commands` تُنشر قائمة الأوامر كاملة تلقائياً
> عند كل إقلاع (استبدال كامل عبر `PUT`)، فلا تبقى أوامر قديمة.

## 🗄️ قاعدة البيانات

كل إعدادات السيرفر مخزّنة في مستند واحد لكل سيرفر (`GuildConfig`) في MongoDB، ويُنشأ تلقائياً
بالقيم الافتراضية عند أول استخدام. لوحة التحكم والبوت يقرآن/يكتبان على نفس القاعدة مباشرة عبر
حزمة `@thez/shared`، لذا أي تعديل من اللوحة ينعكس فوراً على البوت (مع كاش بسيط داخل البوت
لتقليل الضغط على القاعدة).

## ⚠️ ملاحظات ومحدودات معروفة

- **التتبع اللحظي لمكافحة الغزو والرقابة التلقائية (Anti-Nuke / Anti-Spam)** يتم بالذاكرة
  (In-Memory) داخل عملية البوت، لذا يُعاد ضبط العدّادات عند إعادة تشغيل البوت. لبيئة إنتاج
  بمقياس كبير، يُنصح بنقلها إلى Redis.
- **مؤقتات الكابتشا** (الطرد التلقائي بعد مدة) تُستخدم عبر `setTimeout` في الذاكرة، فلن تعمل
  إذا أُعيد تشغيل البوت أثناء فترة الإمهال.
- **أوامر البادئة النصية (Prefix)** تعتمد على ترتيب المعاملات المُعرَّفة بالأمر لاستخراج
  المستخدم/الروم/الرتبة، وهي أبسط من محلل Discord الرسمي لأوامر Slash لكنها تغطي الاستخدام
  الشائع.
- بعض القيم الحساسة (مثل قوائم آيديات المستخدمين المستثناة في مكافحة الغزو) تُدخل كنص عبر
  اللوحة (سطر لكل آيدي) بدل قائمة بحث تفاعلية، لتفادي جلب قوائم أعضاء ضخمة من Discord.

## 🚀 اقتراحات للتوسّع لاحقاً

- ترجمة اللوحة والبوت للإنجليزية (i18n) بجانب العربية.
- نقل حالة الحماية اللحظية إلى Redis لدعم تعدد نسخ البوت (Sharding).
- إضافة صفحة تدقيق (Audit Log) داخل اللوحة لعرض آخر التعديلات.
- إضافة نظام اقتصاد/متجر افتراضي مكمّل لنظام المستويات.
- دعم المزيد من روابط OAuth2 Redirect URLs لبيئات متعددة (تطوير/إنتاج).