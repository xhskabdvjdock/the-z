{
  "name": "the-z-bot",
  "version": "1.0.0",
  "private": true,
  "description": "بوت ديسكورد إداري متكامل مع لوحة تحكم ويب",
  "workspaces": [
    "shared",
    "bot",
    "dashboard"
  ],
  "scripts": {
    "start": "node start.js",
    "build:shared": "npm run build -w shared",
    "dev:bot": "npm run dev -w bot",
    "build:bot": "npm run build -w bot",
    "deploy:commands": "npm run deploy -w bot",
    "clear:commands": "npm run clear -w bot",
    "dev:dashboard": "npm run dev -w dashboard",
    "build:dashboard": "npm run build -w dashboard",
    "build:all": "npm run build:shared && npm run build:bot && npm run build:dashboard",
    "test": "npm run build:shared && npm run build:bot && node --test tests/*.test.cjs",
    "test:only": "node --test tests/*.test.cjs",
    "typecheck:dashboard": "cd dashboard && npx tsc --noEmit",
    "verify": "npm run build:shared && npm run build:bot && npm run typecheck:dashboard && node --test tests/*.test.cjs"
  },
  "license": "MIT"
}
